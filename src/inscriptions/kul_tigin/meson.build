pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
kultigindir = join_paths(pkgdatadir, 'rooti/inscriptions/kul_tigin')

sources = [
  '__init__.py',
  'kul_tigin_page.py',
]

install_data(sources, install_dir: kultigindir)
